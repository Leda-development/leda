import * as React from 'react';
import { Story } from '../Story';
import { Basic } from './Basic';

export const Password = () => (
  <Story title="Password">
    <Basic title="Password" />
  </Story>
);
